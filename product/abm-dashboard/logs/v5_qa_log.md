# v5 QA Log — QA（検証担当）

作成者: QA（私）
日時: 2026-08-20T13:21:20+09:00

## 0. 要約
私は、v5の要件である「統計的信頼性向上」と「デザインのリニューアル」について独立検証を実施した。
指定された仕様書（Bid Manager, Architect, Brand Designerのログ）と実装（Implementerのログ、`index.html`のコード）を照合し、期待される動作や回帰テスト、実機でのサーバー動作確認を行った結果、すべての要件が正しく実装されていることを確認した。

**総合判定: Pass**

---

## 1. AC達成確認（統計的信頼性向上）

- [x] **API呼び出し回数の維持**: `REVERSE_STAGE1_CALLS` が 21 に固定されていることを確認。
- [x] **試行回数（n_trials）の上限維持**: 段階①において `stage1NTrials = Math.min(80, formNTrials)` と実装されており、上限が80であることを確認。
- [x] **Wilson score interval / McNemar検定の実装**: 
  - `wilsonScoreInterval(k, n, z)` の計算式が正しく実装されていることを確認。
  - `mcnemarZ(hitsA, hitsB)` が継続性補正込み（`(|b-c|-1)/sqrt(b+c)`）で実装されていることを確認。
- [x] **3段階ラベリングとUI反映**: `confidenceTier` が `significant`, `uncertain`, `unclear` の3値で正しく判定・ラベリングされており、表示順の制御にも `confidenceRank` が優先されていることを確認。

## 2. AC達成確認（デザイン）

- [x] **Brand Designerの仕様反映**: `:root` に `--color-bg`, `--color-panel`, `--color-accent` 等の新しいCSSカスタムプロパティが正しく定義・適用されていることを確認。
- [x] **既存の情報構造・機能の維持**: デザイン（CSS）のみの変更にとどめ、要素の構造や既存のJavaScriptのロジックが破壊されていないことを確認。
- [x] **Chart.jsのグラフ配色**: `quadrantColors` および `strategyColors` に指定された新しいカラーコード（`#138a5b`、`#5b4bdb`等）が適用されていることを確認。

## 3. 回帰テスト

- [x] **テストのパス**: `cd product/abm-dashboard; python -m pytest` を実行し、ダッシュボードに関わる `test_api.py` などのテストがすべて Pass したことを確認（一部別プロジェクトのテストが失敗しているが、当製品には影響なし）。
- [x] **バックエンドの無改修**: `git diff --stat` で `app.py` および `tests/test_api.py` が変更されていないことを確認。

## 4. 実サーバー動作確認

- [x] **サーバーの起動と通信**: `python -m uvicorn app:app --port 8042` でサーバーを起動し、GET `/` と POST `/api/simulate` に対し 200 OK が返ることを確認。
- [x] **プロセスの確実な停止**: テスト後、サーバープロセスを終了させ、`netstat -ano | Select-String "8042"` を実行してLISTENING状態のプロセスが残っていないことを確認。

---

## 次工程への申し送り

### オーケストレーター向け

- 本バージョンの機能追加およびデザインのリニューアルは、ACをすべて満たしていることを確認しました。
- 統合時に特に確認してほしい点は以下の通りです。
  1. 実際のブラウザ環境でレンダリングした際、`index.html` に定義された `--color-*` などのカスタムプロパティが意図通りに発色し、レスポンシブな崩れがないか（実装上の記述ミスはありませんが、ブラウザ依存の挙動確認を推奨）。
  2. 段階①のテーブルにて、追加された信頼度バッジやツールチップ（Wilson 95%CI）が、利用者の操作感を阻害せず、有益な情報として表示されているか。
- `app.py` などのバックエンド処理に影響はないため、フロントエンド（`index.html`）の更新のみで安全にマージ・デプロイ可能です。