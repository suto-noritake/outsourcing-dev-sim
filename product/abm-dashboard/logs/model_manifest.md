# モデル割当マニフェスト — ABMインタラクティブダッシュボード開発

Phase4（LLMマルチエージェント実験）の設計思想を、実際の自社プロダクト開発プロセスに適用し、
役割ごとに異なるモデル/推論強度を割り当てて開発した記録。各役割は`task`ツールの
sub-agent（`agent_type="general-purpose"`）として実行し、フルツールアクセス（ファイル読み書き・
コマンド実行）を与えた上で、それぞれ自分の視点でログファイルに思考・判断を記録させた。

| 役割 | モデル | reasoning_effort | 実行モード | 担当内容 | ログファイル |
|---|---|---|---|---|---|
| Bid/Go-No-Go Manager | `gpt-5-mini` | low | sync | 要求の実現可能性評価・スコープ確定・受け入れ基準(Acceptance Criteria)作成・リスク指摘・Go/No-Go判定 | `logs/bid_manager_log.md` |
| Architect | `claude-sonnet-5` | high | sync | API契約の確定（エンドポイント/スキーマ）、ファイル構成設計、フロント/バック分担方針、性能ガードレール、テスト計画の策定 | `logs/architect_log.md` |
| Implementer | `gpt-5.3-codex` | high | sync | FastAPIバックエンド・静的フロントエンド(HTML/JS/Chart.js)の実装、依存導入、実サーバー起動による動作検証 | `logs/implementer_log.md` |
| QA / Reviewer | `gemini-3.1-pro-preview` | high | sync | 受け入れ基準の項目別再検証、追加テスト実行、UIバグ発見・修正（Baselineボタンの不具合）、性能計測、最終合否判定 | `logs/qa_log.md` |
| オーケストレーター（本セッション） | `claude-sonnet-5`（本会話のモデル） | — | — | 各ロールへの発注・引き継ぎ、最終統合・実動作確認、リポジトリへのコミット/プッシュ | 本ファイル、コミットログ |

## 選定の意図

- **Bid Manager**: 軽量・高速なモデルで十分な「スコープ確認・チェックリスト作成」という
  比較的定型的な作業に割り当て、コスト効率を重視した（Phase1-3の抽象ABMにおける
  「低ティアでもコスト重視の役割には十分」という発想に対応）。
- **Architect**: 最も設計判断の質が結果全体に波及するため、高性能モデル・高い推論強度を
  割り当てた（Phase4設計での「Architectのモデルティアが高いほど、実装フェーズでの
  隠れた手戻りが減る」という仮説に対応する役割）。
- **Implementer**: コード生成に強みを持つモデルを選定し、高い推論強度で実装品質を確保した。
- **QA**: Architect/Implementerとは異なるベンダー（Google Gemini系）のモデルを採用し、
  同一モデル・同一視点による見落としのリスクを避ける「多様な視点によるレビュー」を意図した。
  実際にUIの実バグ（Baselineボタンがシミュレーションを起動しない）を発見・修正できており、
  この設計意図は実証された。

## 結果サマリ（v1）

- 全4ロールの本物のLLM sub-agent呼び出しを実施（モック無し）。
- 最終成果物: FastAPI + 静的フロントエンドによる「ABMインタラクティブダッシュボード」。
  QA最終判定は **Pass**（軽微なUIバグ1件は当日中に発見・修正済み）。
- 単体テスト: `product/abm-dashboard/tests/test_api.py` 7件、すべてパス（オーケストレーターが
  独立して再実行し確認済み）。

## v2イテレーション（実際のクライアント（経営層）フィードバック対応）

出荷後、実際のクライアント（経営層）から「英語ばかりで使い方が分からない」「パラメータ・結果の
意味が分からない」「サーバー起動が面倒」「図が歪む」という4種の苦情を受け、同じ4ロールパイプライン
で v2 改修を実施した。

| 役割 | モデル | reasoning_effort | 担当内容 | ログファイル |
|---|---|---|---|---|
| Bid Manager | `gpt-5-mini` | low | 苦情をAC1-AC7に整理、19パラメータ・4象限の日本語説明文の下書き作成 | `logs/v2_bid_manager_log.md` |
| Architect | `claude-sonnet-5` | high | Bid Managerの下書きをソースコードと突き合わせて誤りを修正・確定、日本語化方式（表示層のみ）、Chart.js歪み修正方式、Windows起動ランチャー設計を確定 | `logs/v2_architect_log.md` |
| Implementer | `gpt-5.3-codex` | high | `index.html`の日本語化・ヘルプ・チャート修正実装、`launch_dashboard.bat/.vbs`新規作成、README更新 | `logs/v2_implementer_log.md` |
| QA | `gemini-3.1-pro-preview` | high | AC1-AC5+ランチャーの独立検証、回帰テスト再実行 | `logs/v2_qa_log.md` |

### v2で判明した重要な事実: 低ティアモデルのパラメータ説明には実際に誤りがあった

Bid Manager（`gpt-5-mini`, low）が作成した19パラメータの日本語説明下書きには、
`beta`（誤: 競争強度→ 正: 成功確率の急峻さ）、`k1`（誤: 能力係数 → 正: コスト単価係数）、
`gamma`（誤: B社の成長率 → 正: A社（クライアント）の要求エスカレーション率）、
`r_min`（誤: 報酬比率の下限 → 正: 契約打ち切りの評判閾値）、`lam`（誤: 成功感度全般 →
正: 評判のEMA平滑化係数）など、**ソースコードを確認せずに書いたための実際の意味誤り**が
複数含まれていた。さらに `budget_c0`/`budget_c1` はB社のコスト定数ではなくA社がB社へ支払う
予算式の係数であるという、Bid Managerの下書きに元々なかった誤りも含まれていた。

Architect（`claude-sonnet-5`, high）が `outsourcing_sim/params.py`・`model.py`・`simulate.py`
を実際に読み込んで全項目をクロスチェックし、全ての誤りを修正・確定させた。この確定版が
Implementerの実装に採用されている。**「軽量モデルに任せる定型作業」であっても、事実確認を
伴う説明文生成では誤りが混入しうることが実証された。** これはPhase4の「役割ごとの技術リテラシー
（モデルティア）が結果の品質を左右する」という仮説を、実開発プロセスで裏付ける具体例となった。

### v2結果サマリ

- QA最終判定: **Pass**（回帰テスト`test_api.py` 7/7維持を確認、バックエンドAPI契約は変更なし）。
- オーケストレーターが独立して: pytest再実行（7/7）、実サーバー起動＋`/api/simulate`への実HTTPリクエスト
  （英語enumキーが維持されていることを確認）、QAが停止し忘れたテスト用サーバープロセスの検出・停止、
  を実施し確認済み。
- 日本語化は表示層のみに限定し、APIのJSON契約（enum値等）は変更していない
  （既存テストとの後方互換性を維持するための設計判断）。

## v3イテレーション（散布図・分析深度・デザイン改修）

v2出荷後、クライアントから「四象限散布図が見づらい・散らばりが見えない・不要な凡例（保守的/
コスト最適/適応型）が残っている」「分析が甘い」「デザインがダサい」という新たな苦情を受け、
同じ4ロールパイプラインでv3改修を実施した。

| 役割 | モデル | reasoning_effort | 担当内容 | ログファイル |
|---|---|---|---|---|
| Bid Manager | `gpt-5-mini` | low | 苦情をAC1-AC5に整理、散布図修正・感度分析・デザイン刷新の下書き案作成 | `logs/v3_bid_manager_log.md` |
| Architect | `claude-sonnet-5` | high | Bid Manager案をソースコードで検証し**2件の統計的・UX的な不採用判断**を行い確定仕様を作成 | `logs/v3_architect_log.md` |
| Implementer | `gpt-5.3-codex` | high | 散布図・分析UI・感度チェック機能・デザイン刷新の実装、実データでの疎通確認 | `logs/v3_implementer_log.md` |
| QA | `gemini-3.1-pro-preview` | high | 却下された旧設計への回帰がないことの確認を含む全項目の独立検証 | `logs/v3_qa_log.md` |

### v3で判明した重要な事実: 「もっともらしい統計」が二次被害を生むリスクをArchitectが未然に防いだ

Bid Manager（`gpt-5-mini`, low）は、パラメータ感度チェック機能について「3点（基準値の75%/
100%/125%）でPearson相関係数(r)を算出して提示する」という案を、また散布図の重なり対策として
「座標を±0.12ランダムにずらすジッター＋不透明度0.85」という案を作成した。

Architect（`claude-sonnet-5`, high）は`outsourcing_sim`のソースコードを実際に読み込んで検証し、
**両案を統計的・UX的な理由で明確に不採用と判定した**:

- **Pearson r（3点）は不採用**: データ点数n=3・自由度1では、5%水準で有意となるために
  必要な相関係数は`|r|≈0.997`（ほぼ完全な直線関係）に達し、実質的に無意味な数値を「それらしい
  精密さ」で提示することになる。これは「分析が甘い」というクライアントの指摘を、今度は
  「見かけ倒しの厳密さ」という別の問題として再生産するリスクがあると判断し、代わりに
  「低/基準/高の平均純利益＋変化率(%)＋単調性の方向」という、サンプル数に見合った
  誠実な提示方法に置き換えた。
- **座標ジッターは不採用**: 四象限の判定閾値（技術優位度=0、資金体力=5）は意味のある実数の
  境界であり、座標をランダムにずらすと「色（象限）は正しいのに、見た目の位置が閾値をまたいで
  見える」という、クライアントが問題視している「散布図が信用できない」を助長するリスクがある
  と判断。代わりに座標は一切加工せず、不透明度のみ0.55に下げて重なりを可視化し、
  象限境界線（x=0, y=5の破線）を描画するプラグインを新設して対応した。

**この事例は、v2で見つかった「低ティアモデルの事実誤認」とは異なる種類のリスク——
「一見もっともらしいが、サンプル数や表現方法の観点で誤解を招きうる分析・UI設計」——を、
高ティアモデルによる検証工程が実際に防いだ具体例となった。** 単なる事実確認（v2）だけでなく、
「統計的に正直な提示方法か」「UXとして誤解を生まないか」という、より高度な判断が必要な場面でも、
役割分担によるレビュー構造が機能することを示している。

### v3結果サマリ

- QA最終判定: **Pass**（却下された旧設計＝Pearson相関・座標ジッターへの回帰がないことをQAが
  ソースコードで確認、回帰テスト`test_api.py` 7/7維持、バックエンド無改修を確認）。
- オーケストレーターが独立して: pytest再実行（7/7）、`app.py`/`test_api.py`が無変更であることの
  確認、実サーバー起動・API疎通確認、テスト用サーバープロセスの残存がないことの確認、を実施済み。
- 散布図は「色=四象限のみ・形状=戦略のみ」に統一し、Chart.js標準凡例を廃止して
  `#quadrant-legend`パネルのみを凡例の唯一の情報源とした。
- 分析機能として、四象限分布％・戦略別傾向コメント（既存レスポンスから追加通信なしで算出）と、
  オプトインのパラメータ感度チェック（`k1`/`sigma_noise`、各3点、最大6回の追加API呼び出し、
  共通乱数によるノイズ低減）を新設した。
- デザインは、CSSカスタムプロパティによるモダンな配色・フォントスタック・カード装飾に刷新した
  （バックエンドAPI契約・既存テストは無改修）。
