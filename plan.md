# 受託開発シミュレーション（技術力 × 資金モデル）

## 問題設定・アプローチ

「委託元（発注側、資金潤沢・高難度要求）」と「受託会社（開発側、技術力はあるが資金制約あり）」の
関係を、AIエージェントによる開発を前提にモデル化し、多数回シミュレーションすることで
今後の技術受託開発のあり方を分析する。

- **技術力** = モデル性能（能力レベル）× 投入エージェント数
- **資金** = AIクレジット残高（消費速度・調達額）

ユーザーの意向を踏まえ、以下の3段階で進める：

1. **抽象エージェントベースモデル（ABM）**で大量試行し、統計的に有意な傾向を探索する
2. **厳密な実験設計**（パラメータ空間、反復回数、感度分析、検証方法）を並行して整備する
3. 抽象モデルで見つかった「気になる設定」について、**実際のLLMマルチエージェント実験**
   （実際に交渉・実装作業を行わせる）で検証・キャリブレーションする

新規GitHubリポジトリ `outsourcing-dev-sim` を作成し、そこで開発を進める。

---

## Phase 1: 概念・数理モデルの設計（詳細版）

### 市場構造（v1）
1対1固定：委託元 A は1社のみ、受託会社 B も1社。両者が複数ラウンド t = 1..T にわたり
継続的に取引を繰り返す「反復ゲーム」としてモデル化する（市場競争・複数社対応は将来拡張）。

### 状態変数
- **A側**: 累計予算/資金余力 `Budget_A`、B への信頼度（評判）`R_t`、要求難易度 `D_t`
- **B側**: 保有AIクレジット `Funds_B_t`、選択する技術投資 `(m_t, n_t)`
  - `m_t`: 投入するモデル性能ティア（離散: Low / Mid / High / Frontier）
  - `n_t`: 投入エージェント数（整数）

### 直感的な2軸4象限フレームワーク（B社の状態を要約する軸）

13個の詳細パラメータはそのままでは直感的に語れないため、B社（受託会社）の状態を
**「技術優位度」×「資金体力」** という2つの合成指標に集約し、4象限で物語的に語れるようにする。
これは統計的な主成分ではなく、**結果を解釈・可視化するための"読み解きの軸"**として使う。

- **X軸: 技術優位度（Capability Margin）** = `Capability(m, n) − D_t`
  「今受けている案件の難易度に対して、技術力がどれだけ上回っている/下回っているか」
  → 低い（劣位）⇔ 高い（優位）
- **Y軸: 資金体力（Funding Runway）** = `Funds_B_t ÷ 平均バーンレート`
  「今のペースで資金を使い続けた場合、あと何ラウンド持ちこたえられるか」
  → 乏しい（枯渇寸前）⇔ 潤沢（余裕あり）

| | 資金体力：乏しい | 資金体力：潤沢 |
|---|---|---|
| **技術優位度：高い** | ② 宝の持ち腐れ／燃え尽き型<br>（技術はあるが運転資金が続かず倒産リスク大。ユーザー想定の「受託会社B」はここから出発しやすい） | ① 独走勝ち組型<br>（技術力・資金とも優位。案件エスカレーションを積極的に受け、市場を席巻） |
| **技術優位度：低い** | ④ 淘汰予備軍型<br>（技術も資金も苦しく、早期に市場退出しやすい） | ③ 物量型／時間稼ぎ型<br>（技術力不足でもリトライや低ティアモデル大量投入で当面は延命するが、失敗が続けば評判が悪化し③→④へ転落しやすい） |

- シミュレーション上では、各ラウンドでB社のこの2指標をプロットすると
  「①→②（技術投資しすぎて資金が枯渇していく）」「③→④（延命策が尽きて淘汰される）」
  といった**状態遷移の軌跡（フェーズポートレート）**として可視化できる
- Phase 2 の可視化に「技術優位度×資金体力の散布図・軌跡アニメーション」を必須の出力として含める
- Phase 5 のレポートでは、どの戦略（保守的/コスト最適/適応的）がどの象限に留まりやすいか、
  どの象限から抜け出せないか（トラップ）を主要な考察軸とする

### 技術力関数
```
Capability(m, n) = perf(m) * n^α        # α ∈ (0,1): エージェント数の収穫逓減（協調オーバーヘッド）
```
- `perf(m)` はティアごとの性能値（例: Low=1, Mid=2.5, High=4, Frontier=6）
- モデルティアが上がるほど**性能の伸び以上にクレジット単価が上がる**非線形コストを設定し、
  実際のLLM API料金体系の非対称性を反映する

### クレジット消費（実行コスト）
```
CreditCost(m, n, D) = k1 * cost_mult(m) * n * D * noise
```
- `cost_mult(m)`: ティアごとの単価倍率（Frontierほど急増）
- `noise`: 対数正規分布などの乱数（見積り誤差・手戻りの再現）
- 実行中に `Funds_B_t` が枯渇したら、その場で**倒産（シミュレーション終了）**

### 成功確率（技術力 vs 難易度）
```
P_success = sigmoid( β * (Capability(m, n) - D_t) )
```
- 技術力が難易度を上回るほど成功しやすいロジスティック関数
- β は「難易度と技術力の差」に対する成功確率の感度（急峻さ）を制御するパラメータ

### 所要時間
```
Time = time_const * D_t / Capability(m, n) * noise
```

### 検収・報酬・評判更新
- 成功時: B は報酬 `Payment_t`（= A が提示した予算）を得る。失敗時は
  部分報酬（設定可能パラメータ、既定0）のみ
- 評判は指数移動平均で更新: `R_t = λ * R_{t-1} + (1-λ) * success_t`
- **契約継続判定**: 連続失敗回数が閾値を超える、または `R_t` が下限を下回ると
  A が契約を打ち切り、シミュレーション終了

### A側の次ラウンド行動（発注方針の更新）
- 成功時: 難易度をエスカレーション `D_{t+1} = D_t * (1+γ)`（上限あり）、
  予算も難易度と評判に応じて増額 `Budget_{t+1} = c0 + c1 * D_{t+1} * f(R_t)`
- 失敗時: 難易度・予算を据え置き/減額

### B側の意思決定（戦略・スイープ対象）
`(m_t, n_t)` の選び方自体を**比較対象の戦略パラメータ**として複数用意し、
どの戦略が長期生存・利益最大化に有利かを検証する:
- 保守的戦略: 予算内で成功確率を最大化する `(m,n)` を選ぶ（コスト度外視気味）
- コスト最適戦略: 必要最小限の投資に抑え、資金保全を優先する
- 適応戦略: 過去の成功/失敗履歴に応じて投資レベルを調整する（学習的）

### 主要指標（メトリクス）
- 生存ラウンド数（倒産/契約打ち切りまでの期間）、最終利益・資金推移
- ラウンドごとの成功率、難易度到達点（どこまでエスカレーションできたか）
- `(m, n)` 選択と結果の相関、戦略間の生存率・利益率の比較

### 未確定パラメータ（実装時にsweep対象として明示）
`α, β, γ, λ, k1, cost_mult(·), perf(·), 部分報酬割合, 難易度初期値/分布, 初期資金`
これらは固定値で決め打ちせず、実験設計（Phase 3）でレンジを定義してスイープする。

### 将来拡張（v1では対象外）
- 複数B社による入札競争（1対N, N対N）
- A側の複数案件同時発注、B側のリソース配分最適化
- 評判の外部公開（市場全体でのB社評判共有）
- 各社内の役割分業（企画/受託可否判断/設計/実装/QA）と、仕様の曖昧さ・難易度見積り誤差の
  情報非対称性モデル化（Phase4のLLM実験結果を踏まえて、有望であれば抽象ABMにも因子として追加を検討）

モデルの前提・簡略化事項を明文化し、README/DESIGN.md にまとめる。

## Phase 2: 抽象ABMの実装（Python）

- `outsourcing_sim/` パッケージとして実装（agents.py, market.py, project.py, simulate.py 等）
- 1回の案件〜複数ラウンドにわたる市場シミュレーションをステップ実行できるようにする
- モンテカルロ実行（多数の乱数シードで反復）とパラメータスイープ（モデル性能・エージェント数・
  初期資金・案件難易度分布・競合社数などをグリッド/ランダムサンプリングで変化）
- 結果はCSV/Parquetに保存し、pandas/matplotlib(またはseaborn)で集計・可視化するスクリプト/ノートブックを用意

## Phase 3: 実験設計マトリクス（詳細版）

### 仮説（検証対象）
- H1: 技術力が難易度を上回っている限り、失敗要因としては「実装失敗」より
  「資金枯渇（倒産）」の方が支配的になる
- H2: モデルティアを上げるほど成功率は上がるが、コストの非線形増加により
  ある閾値を超えると純利益は低下する（フロンティアモデル濫用は非効率になる）
- H3: エージェント数を増やす戦略は、α（収穫逓減）が小さいほど有効性が下がる
- H4: 「適応戦略」は「保守的戦略」「コスト最適戦略」より長期生存率・利益率で優位

### パラメータ一覧・水準表

| # | パラメータ | 記号 | 種別 | Baseline | スクリーニング水準 (Low/Mid/High) | 集中実験レンジ | 備考 |
|---|---|---|---|---|---|---|---|
| 1 | エージェント数収穫逓減指数 | α | 構造 | 0.6 | 0.3 / 0.6 / 0.9 | 上位選定時に0.05刻みで細分化 | 小さいほどn増加の効果が薄い |
| 2 | 成功確率の感度 | β | 構造 | 3.0 | 1 / 3 / 8 | 同上 | 大きいほど閾値付近で二極化 |
| 3 | 難易度エスカレーション率 | γ | A方策 | 0.15 | 0.05 / 0.15 / 0.3 | 同上 | Aの強欲さ |
| 4 | 評判EMA平滑化係数 | λ | 構造 | 0.7 | 0.5 / 0.7 / 0.9 | 固定（影響小なら screening後に固定） | |
| 5 | 基準クレジット単価係数 | k1 | 構造 | 1.0 | 0.5 / 1.0 / 2.0 | 同上 | 全体コスト水準のスケール |
| 6 | モデルティア単価カーブの急峻さ | cost_curve | 構造 | 二乗的 | 線形 / 二乗 / 三乗 | 上位選定時のみ細分化 | Frontier濫用の非効率性を左右 |
| 7 | 失敗時部分報酬割合 | partial_pay | A方策 | 0.0 | 0.0 / 0.2 / 0.5 | 同上 | |
| 8 | 初期難易度 | D_0 | 環境 | 1.0（基準化） | 0.5 / 1.0 / 2.0 | 同上 | |
| 9 | 初期資金（B） | Funds_0 | 環境 | 中規模案件10件分相当 | 小/中/大 | 同上 | 破産感度に直結 |
| 10 | 評判下限（契約打切閾値） | R_min | A方策 | 0.4 | 0.2 / 0.4 / 0.6 | 固定候補 | |
| 11 | 連続失敗許容回数 | K | A方策 | 3 | 1 / 3 / 5 | 固定候補 | |
| 12 | コスト/時間ノイズの分散 | σ_noise | 構造 | 0.3（対数正規） | 0.1 / 0.3 / 0.6 | 同上 | 見積り誤差の大きさ |
| 13 | **B戦略**（主要因） | strategy | 処置変数 | — | 保守的 / コスト最適 / 適応的 | 全水準を常に含める | 全実験で主軸として保持 |

構造パラメータ・環境パラメータ・A方策パラメータは「モデルの前提」、
B戦略は「比較したい処置（主要因）」として扱いを区別する。

### 実験ステージ

**Stage 0: パイロット試行**
- Baseline設定で n=100〜300 回試行し、主要指標（成功率、生存ラウンド数、最終資金）の
  分散を推定 → 目標CI幅（例: 成功率±2pt, 95%CI）から必要反復数を算出
  （正規近似 `n ≈ (z/E)^2 * p(1-p)`、生存ラウンド数は経験分散から算出）

**Stage 1: スクリーニング設計（要因数が多いため全数総当りは行わない）**
- 12個の構造/環境/方策パラメータに対し **Morris法（Elementary Effects）** または
  **2水準分解能IV型部分実施要因計画（fractional factorial）** を適用し、
  少ない試行数（各設計点 50〜100反復）で「効くパラメータ」を絞り込む
- 出力: 各パラメータの標準化効果量ランキング → 上位4〜5個を「集中実験」対象に選定

**Stage 2: 集中実験（Focused Factorial）**
- Stage1で選ばれた上位パラメータ × B戦略（3水準）の**完全要因計画**を、
  Stage0で決めた反復数（統計的検定力を満たす回数）で実行
- 比較の分散低減のため **共通乱数（Common Random Numbers）** を用いる：
  同じ乱数シード列を戦略間で共有し、ペア比較の分散を下げる

**Stage 3: 統計解析**
- 二値アウトカム（成功/生存）: ロジスティック回帰、要因のオッズ比を報告
- 生存ラウンド数（打ち切りデータあり）: Kaplan-Meier生存曲線 + Cox比例ハザードモデル
- 連続アウトカム（最終利益等）: ANOVA / 線形回帰、交互作用項（特にstrategy×他要因）に注目
- 多重比較・信頼区間・効果量（Cohen's d等）を明記し、有意水準は事前に設定（例: α=0.05、
  多重検定時はBonferroni/BH補正）

**Stage 4: 再現性の担保**
- すべての設計点・乱数シード・パラメータ値をrun manifest（JSON/CSV）として記録
- 実験条件・結果・解釈をリポジトリ内 `docs/experiments/` に記録し、後から再実行可能にする

## Phase 4: LLMマルチエージェント実験（社内複数ロール構造・拡充版）

Phase 2-3 は「A社1体・B社1体」の単純な反復ゲームだったが、Phase 4 の実LLM実験では
**各社の中に複数の役割エージェントが存在する組織構造**として再現し、
情報の欠落・曖昧さ・意思決定の分業が結果にどう影響するかを検証する。

### 委託元 A社内のエージェント構成（例）

| 役割 | 機能 | 資金の関わり | 技術力（モデル）の関わり |
|---|---|---|---|
| 夢を語る人（Visionary/経営層） | 野心的なアイデア・要求水準（難易度の種）を発案する | A社全体の予算枠（Budget_A）を設定する | 技術的な現実性を問わない役なので低ティアで十分。むしろ「野心度」を制御するパラメータの発生源 |
| 企画担当（Planner/PM） | 夢を「企画」レベルの提案書に落とし込む。ただし詳細を詰め切らず、曖昧さ・矛盾が残った状態で次に渡す | 自分の作業自体もクレジットを消費（企画コスト） | モデルティアが高いほど**仕様完成度**（曖昧さの少なさ）が上がる。低ティアだと後工程で手戻りが多発する要因になる |
| 発注・交渉窓口（Procurement Lead） | 中途半端な企画をそのままB社へ提示し、価格・納期を交渉する | 実際にB社へ支払う予算の管理者 | 技術リテラシー（モデルティア）が高いほど、B社の「非現実的な安請け合い」を見抜きやすい（過信によるプロジェクト炎上を抑制） |

### 受託会社 B社内のエージェント構成（例）

| 役割 | 機能 | 資金の関わり | 技術力（モデル）の関わり |
|---|---|---|---|
| 受託可否判断者（Bid/Go-No-Go Manager, 営業・PM相当） | 曖昧な企画を受け取り、真の難易度・リスクを見積もり、受注可否と見積り価格を決める | 見積り作業自体の（小さな）クレジット消費、受注後は契約金額を管理 | モデルティアが高いほど**難易度の見積り誤差が小さくなる**（安請け合い・過小/過大見積りを避けやすい） |
| システム設計者（Architect） | 受注した中途半端な企画を技術設計に落とし込み、曖昧さの一部を仮定で埋める | 設計作業のクレジット消費 | モデルティアが高いほど、実装フェーズでの「隠れた手戻り」が減る（実効難易度の分散を圧縮） |
| 実装者チーム（Implementer(s), n体） | 実際にコードを生成・実行する（Phase1-3の `Capability(m, n)` に対応する層） | 最大のクレジット消費源 | モデルティア `m` × エージェント数 `n` がそのまま技術力になる（従来モデルと同じ） |
| （任意）レビュー/QA担当 | 納品前にレビュー・テストを行い、失敗を事前に検出する | 追加のクレジットを消費するオプション投資 | モデルティアが高いほど検出率が上がる。「投資するか否か」自体がB社の戦略変数になる |

### 検証したいこと

- **情報の非対称性・曖昧さの伝播**: A社の企画担当のモデルティアが低い（仕様が曖昧）ほど、
  B社の受託可否判断者が難易度を見誤りやすくなり、結果的にコスト超過・失敗率が上がるか
- **役割ごとの投資配分のトレードオフ**: 限られた資金を「見積り精度（Bid Manager）」
  「設計品質（Architect）」「実装力（Implementer m,n）」「QA」のどこに配分するのが
  最も生存率・利益率に効くか（Phase1-3の `(m,n)` 一極集中モデルより現実的な配分問題になる）
- **A社側の意思決定リテラシー**: 発注担当の技術リテラシーが低いと、B社の過大な安請け合いを
  見抜けず、結果的にプロジェクト炎上（納期遅延・品質低下）が増えるか

### 実験の進め方

- Phase 2-3（抽象ABM）で見つかった「特に興味深い/不確実性が高い」パラメータ設定を少数選定
- 上記のA社内3ロール・B社内4ロール（Bid Manager, Architect, Implementer×n, 任意QA）を
  実際のLLMエージェントとして構成し、各ロールに割り当てるモデルティアをシナリオごとに変えて実行
- 実際の成果物（コード）の成功可否、消費クレジット（ロール別内訳）、所要時間を計測
- 得られた「仕様完成度→難易度見積り誤差→成功率」の実測関係を使って、
  抽象モデル（Phase1-3）に**曖昧さ・見積り誤差パラメータを新たな因子として組み込むかどうか**を
  Phase5前に判断する（拡張候補としてPhase1の「将来拡張」にも記載）
- 少数回でも良いので、役割ごとのモデルティア配分を変えた比較実験（例: 実装だけ高ティア vs
  設計・見積りに厚く配分）を実施し、「技術力の配分戦略」も比較軸に加える

## Phase 5: 結果まとめ

- Phase 2-4 の結果を統合したレポート（README or docs/report.md）を作成
- 今後の技術受託開発の在り方について考察をまとめる

---

## 注意点・オープンな論点（実装時に随時判断）

- LLM実験は実行コスト・時間がかかるため、少数の代表的設定に絞る
- 抽象モデルのパラメータ（技術力関数g、失敗確率関数、クレジット消費モデル）は
  最初は単純な関数形（線形/シグモイド等）から始め、必要に応じて精緻化する
- 市場競争（複数B社の入札）を最初から入れるか、まず1対1の単純ケースから始めるかは
  実装しながら調整する
